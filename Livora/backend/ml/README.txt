Place trained model files here (same names as in roomate.ipynb):
  roommate_compatibility_model.joblib
  fake_profile_detector.joblib

Copy from your CollegProj folder after training, then restart the Flask API.

Optional: set ROOMMATE_MODEL_PATH and FAKE_PROFILE_MODEL_PATH in .env to use other paths.
